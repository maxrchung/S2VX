#version 330 core
out vec4 fragmentColor;
in vec2 normal;
uniform float lineThickness;
uniform float feather;
uniform float fade;
uniform vec3 color;
void main() {
	float normalLength = length(normal) * lineThickness;
	float featherPartial = lineThickness - feather;
	float alpha = fade;
	if (normalLength > featherPartial) {
		float normalPartial = normalLength - featherPartial;
		alpha = fade - normalPartial / feather;
	}
	fragmentColor = vec4(color.r, color.g, color.b, alpha);
}