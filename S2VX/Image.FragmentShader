#version 330 core
out vec4 fragmentColor;
in vec2 textureCoordinate;
uniform sampler2D image;
uniform float fade;
void main() {
	vec4 textureColor = texture(image, textureCoordinate);
	textureColor.a *= fade;
    fragmentColor = textureColor;
}